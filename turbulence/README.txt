To obtain result, run: julia -t auto -i model.jl

Additional files:
    - plot_turb.jl
    - plotcov.jl
are for plotting diagnostics

Data/ holds training data (we don't include it here because size limit)
